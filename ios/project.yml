name: ShiguangSchedule
options:
  bundleIdPrefix: com.shiguang
  deploymentTarget:
    iOS: "17.0"

settings:
  base:
    SWIFT_VERSION: "5.9"

targets:
  ShiguangSchedule:
    type: application
    platform: iOS
    sources:
      - Sources/ShiguangSchedule
    settings:
      base:
        INFOPLIST_FILE: Sources/ShiguangSchedule/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.shiguang.schedule
        DEVELOPMENT_TEAM: ""
